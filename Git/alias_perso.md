```bash
    s = status
    d = diff --cached
    dl = diff HEAD HEAD^

    a = add --all
    ap = add --all -p

    c = commit
    cm = commit -m
    csm = commit -S -m

    pl = pull --rebase origin
    pld = pull --rebase origin develop
    ps = push origin

    ch = checkout
    chd = checkout develop
    chn = checkout -b

    rb = branch -D

    st = stash
    stp = stash pop
    stc = stash clear

    ri = rebase -i

    f = flow
    rmf = rm -f

```